<!-- spinner loader -->
<div class="spinner-loader <?php if(isset($default_preloader)) :

                // get variable type
                switch (gettype($default_preloader)) :

                    case 'string':
                    case 'number':
                    case 'integer':
                        echo $default_preloader;
                    break;

                    default:
                        var_dump($default_preloader);
                endswitch;

            endif;
        ?>">
    <img src="<?=assets_image("spinner-test4.gif")?>"/>
</div>

<!-- default preloader -->
<div class="default-preloader <?php if(isset($default_preloader)) :

                // get variable type
                switch (gettype($default_preloader)) :

                    case 'string':
                    case 'number':
                    case 'integer':
                        echo $default_preloader;
                    break;

                    default:
                        var_dump($default_preloader);
                endswitch;

            endif;
        ?>">
    <div class="preloader-container">
        <img src="<?=assets_image("BOAME-medium.png")?>"/>
        <div class="preloader-guage">
            <div class="preloader-progress"></div>
        </div>
        <div class="preloader-text">Loading, please wait..</div>
    </div>
</div>
